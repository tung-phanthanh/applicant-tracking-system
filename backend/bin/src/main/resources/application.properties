spring.application.name=ats
spring.profiles.active=h2

server.port=${SERVER_PORT}

# PostgreSQL/MySQL Database Configuration (Defaults for local profile if used)
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:mysql://localhost:3306/ats}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:haianh}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:1234}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA Configuration
spring.jpa.hibernate.ddl-auto=create
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
# spring.jpa.properties.hibernate.dialect is auto-detected from datasource

# JWT Secret
jwt.access-token-expiration-ms=3600000
jwt.refresh-token-expiration-ms=604800000
jwt.secret-key=${JWT_SECRET_KEY:ats-default-secret-key-must-be-very-long-to-be-secure}

spring.mail.host=${MAIL_HOST:localhost}
spring.mail.port=${MAIL_PORT:1025}
spring.mail.username=${MAIL_USERNAME:test}
spring.mail.password=${MAIL_PASSWORD:test}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true

# Frontend URL used in email links
app.frontend-url=${FRONTEND_URL:http://localhost:5173}
# Sender name shown in emails
app.mail.from=${MAIL_FROM:no-reply@ats.local}